import { InvalidFormDirective } from './invalid-form.directive';

describe('InvalidFormDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidFormDirective();
    expect(directive).toBeTruthy();
  });
});
